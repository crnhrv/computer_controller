import 'package:flutter/material.dart';

class MouseControl extends StatelessWidget {
  const MouseControl({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("MOUSE CONTROL CURRENTLY UNAVAILABLE"));
  }
}
